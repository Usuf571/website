# 🚀 Быстрый старт: Управление складом и Аналитика

## ⚡ Минимальная 3-шаговая инструкция

### Шаг 1: Загрузить тестовые данные (опционально)

Откройте консоль браузера (`F12` > `Console`) на странице **admin.html** и скопируйте-вставьте содержимое файла `test_data.js`:

```javascript
// Вставить сюда весь код из test_data.js и нажать Enter
```

Вы увидите сообщение: ✅ Все тестовые данные готовы!

### Шаг 2: Перейти на вкладку "🏢 Склад"

1. Откройте `admin.html`
2. Введите пароль: `admin123`
3. Нажмите на вкладку "🏢 Склад" в меню слева

### Шаг 3: Добавить товар на склад

1. Нажмите кнопку "➕ Добавить остаток"
2. Заполните форму:
   - **Товар**: Выберите из списка (например, iPhone 15)
   - **Количество**: 50 шт
   - **Минимум**: 10 шт
   - **Цена закупки**: 800 сом
   - **Дата**: Сегодня
3. Нажмите "Сохранить"

---

## 📊 Просмотр аналитики

1. Перейдите на вкладку "📊 Аналитика"
2. Выберите период: **За месяц**
3. Вы увидите:
   - 📈 График продаж по дням
   - 🏆 Топ товаров по просмотрам
   - 📉 Статистику трафика
   - 📊 Распределение по категориям

---

## 🔑 Основные функции

### Склад 🏢

| Кнопка | Что делает |
|--------|-----------|
| ➕ Добавить остаток | Открыть форму для добавления товара |
| ✏️ Ред. | Редактировать остаток |
| 🗑️ Удал. | Удалить остаток |
| 📥 Экспорт CSV | Скачать данные в Excel |
| 📄 Экспорт PDF | Скачать отчёт (через печать) |

### Аналитика 📊

| Кнопка | Что делает |
|--------|-----------|
| За неделю | Показать продажи за 7 дней |
| За месяц | Показать продажи за 30 дней |
| За год | Показать продажи за год |
| За всё время | Показать всю историю |
| 📊 Экспорт CSV | Скачать отчёт в Excel |
| 📄 Экспорт PDF | Скачать отчёт (через печать) |

---

## ⚠️ Иконки и статусы

### Статусы товаров на складе

- 🟢 **✅ В наличии** - товара больше, чем минимум
- 🟡 **⚠️ На исходе** - товара меньше или равно минимуму
- 🔴 **❌ Нет** - количество = 0

### Графики и тренды

- 📈 **Растёт** - товар набирает популярность
- ➡️ **Стабильно** - продажи на одном уровне
- 📉 **Падает** - интерес к товару снижается

---

## 💾 Где находятся данные

Все данные автоматически сохраняются в **localStorage** браузера:

```javascript
// Открыть консоль (F12) и выполнить:

// Посмотреть остатки
console.log(JSON.parse(localStorage.getItem('warehouse')));

// Посмотреть историю поступлений
console.log(JSON.parse(localStorage.getItem('warehouseHistory')));

// Посмотреть просмотры товаров
console.log(JSON.parse(localStorage.getItem('pageViews')));

// Посмотреть продажи
console.log(JSON.parse(localStorage.getItem('salesData')));
```

---

## 🎯 Типичный рабочий день

### Утро
1. ☀️ Открыть админку
2. 🏢 Проверить "🏢 Склад" - есть ли уведомления о нехватке
3. 📥 Если необходимо, добавить новые поступления

### День
4. 📊 Периодически проверять "📊 Аналитика" - как идут продажи
5. 🛒 Каждый заказ автоматически записывается

### Вечер
6. 📋 Перед закрытием экспортировать CSV отчёт
7. 💾 Сохранить отчёт на компьютер как резервную копию

---

## 🔄 Жизненный цикл товара

```
Товар добавлен в каталог
           ↓
    [Отслеживаем просмотры]
           ↓
  Добавляем остаток на склад
           ↓
   Отслеживаем продажи
           ↓
  Если количество < минимума
           ↓
        ⚠️ УВЕДОМЛЕНИЕ
           ↓
  Добавляем новое поступление
           ↓
  Остаток обновляется
```

---

## 🆘 Часто задаваемые вопросы

### Q: Как удалить тестовые данные?
A: Откройте консоль (F12) и выполните:
```javascript
localStorage.removeItem('warehouse');
localStorage.removeItem('warehouseHistory');
localStorage.removeItem('pageViews');
localStorage.removeItem('salesData');
```

### Q: Почему данные на графиках отсутствуют?
A: Нужны реальные продажи. Совершите несколько тестовых заказов через сайт.

### Q: Как экспортировать в PDF?
A: Нажмите "📄 Экспорт PDF" → автоматически откроется Print → Ctrl+S → "Сохранить как PDF"

### Q: Куда сохраняются данные?
A: В localStorage браузера. Они сохранятся при закрытии браузера.

### Q: Как перенести данные на другой компьютер?
A: Используйте экспорт CSV. Данные можно импортировать в Excel или обработать скриптом.

### Q: Минимальный остаток не срабатывает?
A: Проверьте, что число в поле "Минимум" больше текущего "Количества".

---

## 📱 На мобильном

Вся функциональность работает и на мобильных устройствах:
- 📲 Сайт адаптивный
- 🖥️ Админка тоже адаптивна (но лучше открывать на ПК для удобства)
- 📊 Графики и таблицы сжимаются для экранов

---

## 🎓 Примеры для обучения

### Пример 1: Добавить iPhone на склад

```javascript
// Открыть консоль (F12) и выполнить:

warehouse.push({
    id: 1,
    productId: 1,          // iPhone 15
    quantity: 50,
    minimum: 10,
    costPrice: 850,
    date: "2025-12-18",
    note: "Оптовый заказ"
});

localStorage.setItem('warehouse', JSON.stringify(warehouse));
console.log('✅ Товар добавлен!');
```

### Пример 2: Получить статистику выручки

```javascript
const sales = JSON.parse(localStorage.getItem('salesData')) || [];
const revenue = sales.reduce((sum, s) => sum + (s.quantity * s.price), 0);
console.log(`Выручка: ${revenue} сом`);
```

### Пример 3: Найти товары на исходе

```javascript
const warehouse = JSON.parse(localStorage.getItem('warehouse')) || [];
const lowStock = warehouse.filter(w => w.quantity <= w.minimum);
console.table(lowStock);
```

---

## 🚀 Готово к использованию!

Система полностью интегрирована и готова к работе. Все данные безопасно хранятся в localStorage и не теряются.

**Версия:** 3.0  
**Статус:** ✅ Полностью рабочая  
**Последнее обновление:** 18 декабря 2025
