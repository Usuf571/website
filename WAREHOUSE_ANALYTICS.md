# 📦 Управление Складом и 📊 Аналитика

## Новые функции (обновление)

### 🏢 УПРАВЛЕНИЕ СКЛАДОМ

Полная система управления остатками товаров с отслеживанием, уведомлениями и историей поступлений.

#### Основные возможности:

1. **Добавление/Редактирование остатков**
   - Выбор товара из каталога
   - Установка количества в наличии
   - Минимальный остаток для уведомлений
   - Цена закупки товара
   - Дата поступления

2. **Отслеживание остатков**
   - Общее количество товаров на складе
   - Товары на исходе (с автоматическими уведомлениями)
   - Товары, которых нет в наличии
   - Стоимость всех товаров на складе

3. **Уведомления при окончании**
   - ⚠️ Автоматические предупреждения при низких остатках
   - ❌ Красные уведомления при отсутствии товара
   - Отображаются в верхней части вкладки
   - Выполняются в реальном времени

4. **История поступлений**
   - Полная история всех поступлений товаров
   - Дата, количество, цена, сумма
   - Возможность удаления записей
   - Сортировка по датам (новые сверху)

5. **Экспорт данных**
   - CSV экспорт текущих остатков
   - PDF экспорт через системную печать (Ctrl+P)
   - Включает все необходимые данные для анализа

#### Как использовать:

1. Перейти в админку (admin.html)
2. Нажать на вкладку "🏢 Склад"
3. Нажать "➕ Добавить остаток"
4. Заполнить форму и сохранить
5. Остатки автоматически обновятся в таблице

#### Хранение данных:

- `warehouse` - текущие остатки товаров в localStorage
- `warehouseHistory` - история всех поступлений в localStorage

---

### 📊 УЛУЧШЕННАЯ АНАЛИТИКА

Комплексная система аналитики с графиками, отчётами и экспортом данных.

#### 1️⃣ Отслеживание просмотров

**Что отслеживается:**
- Каждый открытый товар записывается как просмотр
- Счётчик просмотров сохраняется в localStorage
- Используется для ранжирования топ-товаров

**Как работает:**
```javascript
// При открытии товара:
if (!pageViews[product.id]) pageViews[product.id] = 0;
pageViews[product.id]++;
localStorage.setItem('pageViews', JSON.stringify(pageViews));
```

#### 2️⃣ Отслеживание продаж

**Что записывается:**
- ID товара, количество, цена
- Дата и время покупки
- Используется для графиков и анализа

**Как работает:**
```javascript
// При оформлении заказа:
cart.forEach(item => {
    recordSale(item.id, item.quantity || 1, item.price);
});
```

#### 3️⃣ График продаж по дням

- Показывает объем продаж за каждый день
- Работает с фильтром по периодам (неделя, месяц, год, всё время)
- Интерактивные столбцы с информацией при наведении
- Красивая градиентная визуализация

#### 4️⃣ Топ товаров по просмотрам

- Рейтинг товаров по количеству просмотров
- Показывает тренд (📈 Растёт, ➡️ Стабильно, 📉 Падает)
- Примерный счётчик лайков (случайный)
- Top 10 товаров

#### 5️⃣ Статистика трафика

Четыре ключевых метрики:

- **📊 Всего продаж** - количество проданных единиц
- **💰 Выручка** - общая сумма продаж в сомах
- **🔄 Средний чек** - средняя стоимость одной покупки
- **📝 Транзакций** - количество совершённых заказов

#### 6️⃣ Товары по категориям

- Визуальные полосы распределения товаров
- Процент от максимума в каждой категории
- Общее количество товаров в каждой категории

#### 7️⃣ Экспорт данных

**CSV экспорт включает:**
- Общую статистику (товары, цены, отзывы, просмотры)
- Топ-10 товаров с количеством просмотров
- Форматирован для Excel/Google Sheets

**PDF экспорт:**
- Используйте встроенную печать (Ctrl+P / Cmd+P)
- Сохраните как PDF - получится красивый отчёт
- Включает все графики и таблицы

#### Как использовать аналитику:

1. Перейти в админку (admin.html)
2. Нажать на вкладку "📊 Аналитика"
3. Выбрать нужный период (неделя/месяц/год/всё время)
4. Просмотреть графики и статистику
5. При необходимости, экспортировать данные

#### Хранение данных аналитики:

- `pageViews` - счётчик просмотров товаров по ID
- `salesData` - полная история всех продаж
- Все данные сохраняются в localStorage

---

## 📈 Структура данных

### Warehouse (Склад)

```javascript
{
    id: 1,
    productId: 1,        // ID товара из каталога
    quantity: 50,        // Текущее количество
    minimum: 5,          // Минимальный остаток
    costPrice: 800,      // Цена закупки
    date: "2025-12-18",  // Дата поступления
    note: "Опт",         // Примечание
    createdAt: "ISO 8601 timestamp"
}
```

### Warehouse History (История поступлений)

```javascript
{
    id: 1,
    productId: 1,
    quantity: 50,
    costPrice: 800,
    date: "2025-12-18",
    totalSum: 40000,     // quantity * costPrice
    note: "Опт"
}
```

### Sales Data (Данные о продажах)

```javascript
{
    id: 1,
    productId: 1,
    quantity: 2,
    price: 1099,
    date: "2025-12-18",
    time: "14:30:45"
}
```

### Page Views (Просмотры)

```javascript
{
    "1": 25,   // productId: количество просмотров
    "2": 18,
    "3": 42
}
```

---

## 🔧 API Функции

### Склад

```javascript
// Добавить/редактировать остаток
openWarehouseModal(editId = null)

// Закрыть модал остатков
closeWarehouseModal()

// Удалить остаток
deleteWarehouseItem(id)

// Удалить из истории
deleteWarehouseHistory(id)

// Обновить все данные склада на странице
renderWarehouseData()

// Экспортировать в CSV
exportWarehouse()

// Экспортировать в PDF (через Print)
exportWarehousePDF()
```

### Аналитика

```javascript
// Записать просмотр товара
trackPageView(productId)

// Записать продажу
recordSale(productId, quantity, price)

// Обновить аналитику на странице
updateAnalytics()

// Отрендерить всю статистику
renderAnalytics()

// Отрендерить график продаж
renderSalesChart(data)

// Отрендерить топ товаров
renderTopProducts()

// Отрендерить статистику трафика
renderTrafficStats(data)

// Экспортировать аналитику в CSV
exportAnalyticsCSV()

// Экспортировать в PDF (через Print)
exportAnalyticsPDF()
```

---

## 💡 Примеры использования

### Добавить товар на склад (JavaScript)

```javascript
// Открыть форму добавления
openWarehouseModal();

// Или программно добавить:
warehouse.push({
    id: warehouse.length + 1,
    productId: 1,
    quantity: 100,
    minimum: 10,
    costPrice: 850,
    date: new Date().toISOString().split('T')[0],
    note: "Первый заказ"
});
localStorage.setItem('warehouse', JSON.stringify(warehouse));
renderWarehouseData();
```

### Получить товары на исходе

```javascript
// Товары, чьё количество <= минимума
const lowStock = warehouse.filter(w => w.quantity <= w.minimum);
console.log(lowStock);
```

### Получить общую стоимость склада

```javascript
const totalValue = warehouse.reduce((sum, w) => sum + (w.quantity * w.costPrice), 0);
console.log(`Стоимость склада: ${totalValue} сом`);
```

### Получить топ товаров по просмотрам

```javascript
const topProducts = products
    .map(p => ({ ...p, views: pageViews[p.id] || 0 }))
    .sort((a, b) => b.views - a.views)
    .slice(0, 10);
console.table(topProducts);
```

### Получить выручку за период

```javascript
const startDate = new Date('2025-12-01');
const endDate = new Date('2025-12-31');

const revenue = salesData
    .filter(s => {
        const saleDate = new Date(s.date);
        return saleDate >= startDate && saleDate <= endDate;
    })
    .reduce((sum, s) => sum + (s.quantity * s.price), 0);

console.log(`Выручка: ${revenue} сом`);
```

---

## 📱 Адаптивность

Все новые компоненты полностью адаптированы для:
- 💻 Десктопов (1920px+)
- 🖥️ Планшетов (768px - 1024px)
- 📱 Телефонов (320px - 768px)

Используются:
- CSS Grid с `auto-fit` и `minmax()`
- Flexible Layout для кнопок и управления
- Сокращение размеров шрифтов на мобилах
- Стек элементов в колонку на мобилах

---

## 🐛 Дебаггинг

Проверить данные в консоли:

```javascript
// Посмотреть все остатки
console.log(JSON.parse(localStorage.getItem('warehouse')));

// Посмотреть историю поступлений
console.log(JSON.parse(localStorage.getItem('warehouseHistory')));

// Посмотреть просмотры
console.log(JSON.parse(localStorage.getItem('pageViews')));

// Посмотреть продажи
console.log(JSON.parse(localStorage.getItem('salesData')));
```

---

## 🔐 Безопасность

- Все данные хранятся локально в localStorage (браузер пользователя)
- При закрытии браузера данные остаются
- Очистка localStorage удалит все данные
- Рекомендуется регулярно создавать резервные копии (экспорт)

---

## 📝 Обновления структуры файлов

Добавлены вкладки в `admin.html`:
- `<div id="warehouseTab">` - Управление складом
- Обновлена `<div id="analyticsTab">` - Расширенная аналитика

Новые модали:
- `<div id="warehouseModal">` - Форма добавления остатков

Функции в `admin.js` (180+ строк):
- Управление складом (CRUD, уведомления, экспорт)
- Расширенная аналитика (графики, отчёты)

Функции в `main.js` (30+ строк):
- Отслеживание просмотров товаров
- Регистрация продаж при оформлении заказа

Стили в `css/styles.css` (150+ строк):
- Оформление вкладок склада и аналитики
- Адаптивные сетки для мобилов
- Красивые карточки и уведомления

---

**Версия:** 2.0  
**Дата обновления:** 18 декабря 2025  
**Статус:** ✅ Готово к использованию
